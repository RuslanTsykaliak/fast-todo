    /// SIMPLE FORM TO GET TODOS
    // <main className="flex min-h-screen flex-col items-center justify-between p-24">
    //   <div>
    //     {todos && todos.length > 0 ? (
     //     // todos.map((todo: Todo) => (
    //         <div key={todo.id}>
    //           <h2>{todo.title}</h2>
    //           <p>{todo.description}</p>
    //           <p>Priority: {todo.priority}</p>
    //         </div>
    //       ))
    //     ) : (
    //       <div>No todo to display</div>
    //     )}
    //   </div>
    // </main>